import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFDC2626); // Rich Crimson Red
  static const Color primaryLight = Color(0xFFEF4444); // Vibrant Red
  static const Color accent = Color(0xFFF59E0B); // Amber for ratings/stars

  // Background & Surface Colors
  static const Color scaffoldBg = Color(0xFF121212); // Deep rich dark background
  static const Color cardBg = Color(0xFF1E1E1E); // Elevated dark surface for cards

  // Text Colors
  static const Color textMain = Color(0xFFF8FAFC); // Crisp off-white for main text
  static const Color textMuted = Color(0xFF9CA3AF); // Soft gray for secondary text

  // Status Colors
  static const Color verifiedIcon = Color(0xFF38BDF8); // Keeps the standard blue tick vibe
  static const Color sponsoredBg = Color(0xFF451A1A); // Dark rich red/brown base
  static const Color sponsoredText = Color(0xFFFCA5A5); // Light red for contrast

  // Expanded Section Colors
  static const Color expandedBg = Color(0xFF262626); // Slightly lighter than card base for depth
  static const Color expandedText = Color(0xFFE2E8F0); // Light text
}